server:
  port: 81

spring:
  application:
    name: base-auth
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
management:
  endpoints:
    web:
      exposure:
        include: '*'